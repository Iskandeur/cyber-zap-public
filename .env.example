# =============================================================================
# CyberZap Environment Configuration
# =============================================================================
# Copy this file to .env and customize the values
# =============================================================================

# -----------------------------------------------------------------------------
# N8N Configuration
# -----------------------------------------------------------------------------
N8N_HOST=localhost
N8N_PROTOCOL=http
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme
WEBHOOK_URL=http://localhost:5678/
TIMEZONE=Europe/Paris

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_password

# -----------------------------------------------------------------------------
# WAHA - WhatsApp API (Optional)
# -----------------------------------------------------------------------------
# Uncomment and configure if using WhatsApp integration
# WAHA_API_KEY=your_waha_api_key

# -----------------------------------------------------------------------------
# External API Keys (Configure in n8n UI)
# -----------------------------------------------------------------------------
# These are configured directly in n8n credentials, not environment variables:
# - OPENAI_API_KEY or other LLM provider keys
# - JINA_API_KEY for article scraping
# - TELEGRAM_BOT_TOKEN for Telegram alerts
# - GOOGLE_SHEETS_OAUTH for database storage
